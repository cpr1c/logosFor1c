#Область Логирование

// Хранит параметры логирования для использования на клиенте
Перем лПараметрыЛогирования Экспорт;

// Хранит сообщения логирования на клиенте, если добавлен способ логирования "Память"
Перем лСообщенияЛогирования Экспорт;

#КонецОбласти

